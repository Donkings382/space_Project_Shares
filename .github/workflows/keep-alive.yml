name: Keep Render Backend Alive

# Ping every 5 minutes to prevent free Render instance from sleeping
on:
  schedule:
    - cron: "*/5 * * * *" # every 5 minutes
  workflow_dispatch: # allows manual trigger from GitHub UI

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render backend
        run: |
          echo "Pinging https://spacex-backend-yxlo.onrender.com/health ..."
          status=$(curl -s -o /dev/null -w "%{http_code}" --max-time 30 https://spacex-backend-yxlo.onrender.com/health || echo "000")
          echo "Response status: $status"
          if [ "$status" -lt 200 ] || [ "$status" -ge 300 ]; then
            echo "Health check failed or timed out"
            exit 1
          else
            echo "Health check successful"
          fi
